function Ressource(name, number, base_cost, current_cost) {
    this.name = name;
    this.number = number;
    this.base_cost = base_cost;
    this.current_cost = current_cost;
}

var timeInterval = 50;
var res = new Ressource("clicker", 0, 10, 10);

window.setInterval(function() {

},timeInterval)